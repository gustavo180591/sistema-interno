{% extends 'base.html.twig' %}

{% block title %}Bienvenido al Sistema Interno{% endblock %}

{% block body %}
<!-- Hero Section -->
<section class="py-5 text-center bg-light">
    <div class="container py-5">
        <div class="row">
            <div class="col-lg-8 mx-auto">
                <h1 class="display-4 fw-bold mb-4">Bienvenido al Sistema de Gestión Interna</h1>
                <p class="lead mb-4">Gestiona tus tickets, sigue el estado de tus solicitudes y mantente actualizado con todas las novedades de la empresa en un solo lugar.</p>
                <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                    <a href="{{ path('app_login') }}" class="btn btn-primary btn-lg px-4 me-sm-3">Iniciar Sesión</a>
                    <a href="{{ path('app_register') }}" class="btn btn-outline-secondary btn-lg px-4">Registrarse</a>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="py-5">
    <div class="container">
        <div class="row g-4 py-5">
            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="bg-primary bg-opacity-10 rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 60px; height: 60px;">
                            <i class="bi bi-ticket-detailed fs-3 text-primary"></i>
                        </div>
                        <h3>Gestión de Tickets</h3>
                        <p class="text-muted">Crea y sigue el estado de tus tickets de soporte de manera sencilla y eficiente.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="bg-primary bg-opacity-10 rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 60px; height: 60px;">
                            <i class="bi bi-shield-lock fs-3 text-primary"></i>
                        </div>
                        <h3>Seguridad</h3>
                        <p class="text-muted">Tus datos están protegidos con los más altos estándares de seguridad.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body text-center p-4">
                        <div class="bg-primary bg-opacity-10 rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 60px; height: 60px;">
                            <i class="bi bi-clock-history fs-3 text-primary"></i>
                        </div>
                        <h3>Disponibilidad 24/7</h3>
                        <p class="text-muted">Accede a la plataforma en cualquier momento y desde cualquier dispositivo.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="bg-primary text-white py-5">
    <div class="container py-5 text-center">
        <h2 class="mb-4">¿Listo para comenzar?</h2>
        <p class="lead mb-4">Únete a nuestro sistema y descubre una nueva forma de gestionar tus solicitudes.</p>
        <a href="{{ path('app_register') }}" class="btn btn-light btn-lg">Crear una cuenta</a>
    </div>
</section>

<!-- Bootstrap Icons -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">

<style>
    body {
        font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    }
    .hero {
        background: linear-gradient(135deg, #4b6cb7 0%, #182848 100%);
        color: white;
    }
    .card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
    }
</style>
{% endblock %}
